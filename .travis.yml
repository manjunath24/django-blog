language: python
python:
  - "2.6"
  - "2.7"
  - "3.2"
  - "3.3"
# command to install dependencies
install:
  - "pip install -r requirements.txt"

env:
    MYAPP_SECRET_KEY=travis

# command to run tests
script: nosetests
